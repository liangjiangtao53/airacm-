# 设计系统 — 机务在线学习平台

两套前端(Flutter 双端 App + uni-app 微信小程序)的**视觉单一来源**。
任何一端实现都必须对齐这里的 token,保证视觉一致。

视觉定调:**绿色航空基调,升级专业感**。保留机务绿主色,去掉模板化的「统一圆角灰绿卡片网格」,引入清晰层级、克制留白、专业字体。

---

## 1. 颜色 token

```
--color-brand:        #1F7A52   /* 机务绿,主色:导航、主按钮、选中态 */
--color-brand-deep:   #145C3C   /* 深绿:按下态、强调 */
--color-brand-tint:   #E8F3EC   /* 浅绿:选中背景、徽章底,克制使用 */
--color-accent:       #1F7A52   /* 单一强调色,不引入第二装饰色 */

--color-text:         #1A1A1A   /* 正文主色 */
--color-text-sub:     #5B6B62   /* 次要文字 */
--color-text-mute:    #8A968F   /* 占位、辅助 */

--color-surface:      #FFFFFF   /* 卡片/主面 */
--color-bg:           #F6F8F7   /* 页面底,极浅灰绿 */
--color-border:       #E2E8E4   /* 1px 描边,代替重圆角卡 */

--color-success:      #1F7A52
--color-warning:      #C7821A   /* 待支付等 */
--color-danger:       #C0392B   /* 失败/取消/余额不足 */
```

对比度铁律:正文文字对背景 ≥ 4.5:1。`--color-text-mute` 不得用于正文,仅辅助。

## 2. 字体

```
中文:  "PingFang SC", "Noto Sans SC", "Microsoft YaHei"  (不裸用 system-ui)
数字/价格:  "DIN Alternate", "Inter" tabular-nums  (价格、余额、订单号用等宽数字对齐)
```

字号阶(移动优先):

```
--text-h1: 24px / 600   页面主标题(英语学习)
--text-h2: 18px / 600   区块标题(参考、我的课程)
--text-body: 16px / 400  正文(最小正文 16px,不得更小)
--text-sub: 14px / 400   次要信息(章节数/课时数)
--text-cap: 12px / 500   徽章、标签
--text-price: 20px / 600 价格(等宽数字)
```

## 3. 间距 / 圆角

```
--space: 4 / 8 / 12 / 16 / 24 / 32   /* 8 的倍数为主,4 用于微调 */
--radius-card: 10px    /* 卡片,统一但不夸张 */
--radius-btn: 8px
--radius-pill: 999px   /* 徽章/标签 */
```

留白纪律:区块之间 24,卡片内 16,行间 12。不靠装饰填空,空了就用更好的内容或留白。

## 4. 组件规范

- **按钮**:主按钮 = 实心机务绿 + 白字;次按钮 = 描边;文字按钮 = 仅文字。高度 44px(触控最小)。
- **卡片**:白底 + 1px `--color-border` + `--radius-card`,**禁止彩色左边框、禁止统一大圆角网格**。卡片必须承载真实交互或信息,不做装饰网格。
- **徽章**:pill 形,`--color-brand-tint` 底 + 深绿字(已开通),`--color-warning`(待支付),`--color-danger`(失败)。
- **课时访问标识**:试听/付费/会员/密码 用文字徽章,不用 emoji 图标。
- **导航**:底部 tab(移动)= 学习/课程/我的;小程序 tabBar 同结构。约定优先,不创新导航。

## 5. 去 AI 模板感(硬规则)

现有 mock 的「模块全是统一圆角灰绿卡片网格」是模板感来源,重做时禁止:

1. 紫蓝渐变 / 第二装饰色
2. 三列等宽「图标圆圈+标题+两行描述」特征网格
3. 所有元素统一大圆角
4. 装饰性圆点/波浪分割/漂浮色块
5. emoji 当设计元素
6. 彩色左边框卡片
7. 全局居中对齐
8. system-ui 当主字体

正确做法:用**字号层级 + 留白 + 单一机务绿**建立秩序;参考模块用清单/分组而非装饰卡网格;每个区块只做一件事。

## 6. 响应式 / 无障碍

- 移动优先(主形态)。小程序按机型宽度自适配;Flutter 适配手机 + 平板(平板两栏)。
- 触控目标 ≥ 44px。
- 课时列表、课程卡支持长内容截断(课程名最长两行省略号)。
- 颜色不作为唯一信息载体(状态用文字徽章 + 颜色双重表达,照顾色弱)。
- 关键操作(充值/支付/购课)有明确加载态和结果反馈,不静默。
